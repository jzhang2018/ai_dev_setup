---

- name: Install Local LLM DevOps Agent Stack on RHEL 9
  hosts: agentic_sandbox_node
  gather_facts: true
  
  tasks:
    - name: Pre-requisites
      ansible.builtin.include_tasks: pre_requisites.yml

    - name: Install LangChain
      ansible.builtin.include_tasks: install_langchain.yml


